overwrite: true
schema:
  - https://4xgfahcqmvfmti2gv2rfwhflxe.appsync-api.us-east-2.amazonaws.com/graphql:
      headers:
        x-api-key: 'da2-nujzzwmwo5hftcmaj7axynomui'
documents: ./src/graphql/**/*.gql
generates:
  src/graphql/generated/schema.ts:
    plugins:
      - add:
          content: '/* eslint-disable */'
      - typescript
      - typescript-operations
      - typescript-react-apollo
    hooks:
      afterOneFileWrite:
        - prettier --write
    config:
      noSchemaStitching: true
      withHooks: true
  graphql-schema.json:
    plugins:
      - introspection
    config:
      minify: false
