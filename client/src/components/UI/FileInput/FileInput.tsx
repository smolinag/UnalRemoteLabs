import React from 'react';

interface Props {
  instructionText: string;
  fileName: string;
}

const FileInput: React.FC<Props> = ({instructionText, fileName}) => {

  return(
    <div>
      <div className={}>

      </div>
    </div>
  )
}
export default FileInput;